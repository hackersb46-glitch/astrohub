# PROACTIVITY - 2026-05-20

## v5.14 → v5.15 开发日志

### 变更
- v5.14: 修复 IIFE 边界、删除 HLS 推流卡片、修复控制台视频区
- v5.15: `/hik` 独立路由页面 + 自动连接设备
  - 创建 hik.html（WASM SDK 官方方式）
  - main.py 添加 `/hik` 路由（必须在 create_app 内，4 空格缩进）
  - 页面自动从 `/api/v1/devices` 获取在线设备，自动登录 + 播放
  - 主页面导航栏添加"海康预览"链接
  - hik.html 完整复制 header + nav + CSS（按钮改为 `<a>` 链接）

### 待老板验证
- 浏览器访问 http://127.0.0.1:10280/hik 确认自动获取图像
- 导航栏所有按钮可点击跳转

### 教训（已记录 ERRORS.md）
1. 修改 Python 代码必须先确认函数层级
2. 复制 HTML 必须用 DOM 开闭配对
3. 独立页面导航用 `<a>` 不用 `<button>`
4. 代码存在 ≠ 功能正常
5. 复杂脚本写 .py 文件，不用 python -c

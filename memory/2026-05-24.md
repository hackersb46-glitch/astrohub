# 2026-05-24 工作记录

## 完成任务

### 1. 高级功能界面 v5.31
- 左中右三栏布局（25%/55%/20%）
- 左侧：测试项目 + 操作按钮
- 中间：实时画面
- 右侧：日志 + 结果
- 文件：`src/web/includes/advanced.html`

### 2. PTZ Limit 修复
- 稳定检测：精确相等（移除偏差容忍）
- 连续20点：STABLE_POINTS_REQUIRED=20
- HOME验证：严格等于 (1800, 450, 10)
- 翻转判断：严格下降
- 预置点：set_preset(pan, tilt, zoom, name)
- 文件：`src/advanced/limit.py`, `src/ptz/isapi/ptz.py`

### 3. 数据存储修复
- API 改用 store.py
- 路径：data/devices/{device_id}/
- 完全符合 DATA_STORAGE.md
- 文件：`src/api/router.py`

## 测试结果
- 功能探测：21/21 支持
- 限位：P(0~3600), T(-200~900), Z(10~320)
- 翻转：true
- 稳定：true

## 教训
1. 文档要求"位置0误差"必须严格精确相等
2. 存储路径必须符合设计文档
3. 前端需要支持多种结果格式
# 2026-05-24 记忆

## 完成工作

### P5.1 Continuous Move 测试
- ✅ 测试通过
- Pan位移: 1040 (1800→760)
- Tilt位移: 373 (450→823)
- HOME恢复: 成功

### P5.2 Absolute Move 测试
- ✅ 测试通过
- **关键发现**: 海康ISAPI不接受浮点数坐标，必须转为整数
  - `pan=1850.0` → HTTP 400 ❌
  - `pan=1850` → HTTP 200 ✅
- 修复: `target_pan = int(start_pos.get("pan", 1800)) + 50`

### Speed.py 修复
- ✅ 方向统一为正负值 (1/-1)
- ✅ Zoom从limit_map读取zoom_min/zoom_max
- ✅ 起始位置统一使用HOME

## 关键教训

### 1. ISAPI坐标必须是整数
所有PTZ坐标参数必须 `int()` 转换，浮点数会导致HTTP 400错误。

### 2. 设备状态检测
- `wait_stable()` 检测20点位置不变
- 但不保证位置是预期的HOME
- 需要先检查位置是否正确再执行测试

### 3. 测试隔离
- 连续测试之间需要确保设备回到已知状态
- 使用 `goto_preset(10)` 后等待足够时间（5秒）

## 技术细节

### 海康ISAPI绝对移动格式
```xml
<PTZData version="2.0" xmlns="http://www.hikvision.com/ver20/XMLSchema">
  <AbsoluteHigh>
    <azimuth>1850</azimuth>  <!-- 必须是整数 -->
    <elevation>500</elevation>
    <absoluteZoom>10</absoluteZoom>
    <speed>50</speed>
  </AbsoluteHigh>
</PTZData>
```

### 设备信息
- IP: 192.168.5.72:80
- 用户名: admin
- 密码: Nftw1357
- HOME预置点: 10 (pan=1800, tilt=450, zoom=10)

## 待办
- [ ] P5.3 Relative Move (评审不做要求)
- [ ] 完整一键检测测试

# 2026-06-15 开发记录

## v7.15 开发（03:35-04:10）

### 任务
1. startup 脚本生成 localhost.json
2. SADP 发现速度优化（7.5s → 3.3s）
3. 设备连接逻辑改进 + 添加/删除按钮交互式
4. 本机信息仪表盘显示
5. 手动添加设备模态框
6. 弹窗改为网页级别

### 完成项
- ✅ startup.py 生成 data/reports/localhost.json
- ✅ SADP 发现超时从 10s 降到 3s，使用回调机制
- ✅ 本机信息 API `/api/v1/localhost`
- ✅ 前端本机信息卡片（仪表盘风格）
- ✅ 添加/删除按钮交互式
- ✅ 网页级别弹窗 showAlert/showConfirm

---

## v7.16 开发（04:14-04:17）

### 问题
1. 设备列表出现 2 个重复记录
2. SADP 发现网关问题
3. 设备名称显示错误

### 修复
- 按 IP 合并重复设备
- 简化名称为 `PTZ-{ip}`
- E2E 测试通过

---

## v7.17 精确修复（04:18-04:33）

### 根本问题
设备存储使用 IP 作为 MAC，导致重复存储：
- `data/devices/192.168.5.72/`（错误）
- `data/devices/240f9b764193/`（正确）

### 修复方案
1. **router.py**：register_device 必须从 SADP 发现获取真实 MAC
2. **ptz_manager.py**：save_credentials 拒绝无效 MAC
3. 删除错误存储目录 `192.168.5.72`

### 验证结果
- 设备数量：1 个（✅）
- MAC：`240f9b764193`（✅ 正确格式）
- 名称：`4k 32X DC`（✅）
- 网关：`192.168.5.1`（✅）

### 文档更新
创建 `documents/DATA_STORAGE_SPEC.md`：
- 设备标识统一使用 MAC 地址
- MAC 格式统一为无分隔符小写
- 禁止使用 IP 作为设备标识

---

## 关键教训

### 1. SADP 发现功能
- SADP 发现本身就能获取 MAC、IP、网关
- 设备必定有有效 MAC（无 MAC 无法入网）
- 不应该有任何"无 MAC 设备"的逻辑

### 2. 存储规范
- 设备数据目录必须按 MAC 组织
- 禁止按 IP 创建额外目录
- 所有 MAC 格式统一：无分隔符小写（如 `240f9b764193`）

### 3. 禁止打补丁
- 发现问题必须找到根因
- 精确修复，不是在表面打补丁
- 例如：设备重复不是在合并逻辑修补，而是修复存储逻辑

---

## E2E 测试结果
- v7.15：60/60 PASS
- v7.16：60/60 PASS
- v7.17：60/60 PASS

## Git 提交
- v7.15：已提交
- v7.16：82d268b
- v7.17：已提交（包含 DATA_STORAGE_SPEC.md）
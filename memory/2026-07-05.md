# 2026-07-05 记忆

## 主控台界面重构 v8.39 ✅

### 完成的修改
1. **运动控制 - 中心按钮改为归位**
   - PTZ方向键中心按钮从停止(●)改为归位(⌂)
   - 调用 `ptzHome()` 回到10号预置点HOME
   - 删除预置点下方"归位"按钮

2. **运动控制 - OSD模块移动**
   - 标题改为"信息显示"
   - 保留两个开关：OSD(Info OSD) + PTZ
   - 从右侧画面控制区完整移动到左侧运动控制区
   - 统一ID：`infoOsdSwitch` / `ptzOsdSwitch`

3. **媒体操作 - 删除框选分析**
   - 移除"框选分析"section（白平衡/对焦按钮）
   - 框选功能现集成到白平衡/对焦的"局部"模式

4. **画面控制 - 删除Info OSD**
   - 右侧面板不再有OSD控制区
   - 所有OSD开关都在运动控制区

5. **白平衡/对焦模式重构**
   - 标签从"白平衡模式"改为"白平衡"/"对焦"
   - 3个选项：自动、手动、局部
   - 局部模式：自动切换到内置手动模式
   - 删除手动模式前置要求
   - 框选按钮 + 定时输入框（分钟，0=不执行）
   - 点击框选后显示停止按钮

6. **选区记忆功能**
   - 白平衡和对焦选区分别存储到localStorage
   - key: `region_whitebalance` / `region_focus`
   - 存储格式: `{x, y, w, h}` (归一化坐标)
   - 下次框选时恢复上次的选区

7. **触发与更新常量**
   - 更新 constants.py 版口号：v8.39
   - 更新 HEARDBEAT.md：v8.39 主控台UI重构完成

8. **测试验证**
   - 22项PASSED，0FAILED
   - 测试脚本 `e2e_v838_ui.py` 已更新至 v8.39

---
## v8.41 方案B：用户中断停止功能 ✅（2026-07-04）

### 修改文件
1. **whitebalance.py** — `_interrupted` 标志 + `_interrupt()` 方法 + run()中断检查（写入best R/B + 锁定模式 + yield interrupt事件）
2. **autofocus.py** — `_interrupted` 标志 + `_interrupt()` 方法 + run()中断检查（停止移动 + yield interrupt事件）
3. **router.py** — 全局变量 `_wb_searcher`/`_focus_searcher` + 新增 `/api/v1/vision/whitebalance-stop` + `/api/v1/vision/focus-stop` 端点
4. **console.html** — emoji按钮（🔲✅🛑）+ 停止按钮移到确认右侧 + 按钮状态管理 + 停止函数调用新端点 + operationLog('用户停止')
5. **index.html** — SSE监听 interrupt 事件 + 搜索启动时启用停止按钮 + done/error/interrupt/catch中恢复按钮灰色
6. **e2e_v838_ui.py** — 测试适配emoji按钮 + disabled状态检查 + UTF-8编码修复（sys.stdout.reconfigure）

### 按钮状态逻辑
| 按钮 | 条件 | 状态 |
|------|------|------|
| 框选 🔲 | 始终 | 可用 |
| 确认 ✅ | 定时值>0 且 定时器未运行 | 可用 |
| 确认 ✅ | 定时值<=0 或 定时器运行中 | 灰色不可用 |
| 停止 🛑 | 搜索流程运行中（SSE流中） | 可用 |
| 停止 🛑 | 搜索未启动 | 灰色不可用 |

### 备份
`backup_20260705_0327/`

---
## v8.41 UI布局调整（2026-07-05）

### 需求
1. 白平衡/对焦局部模式下：定时输入框 + 停止 + 确认按钮并列一排
2. 按钮缩小15%
3. 定时输入框缩短20%
4. 变焦功能恢复（速度滑动条下方）
5. 变焦按钮改为文字"变焦+"/"变焦-"，高度50%
6. Home按钮改为emoji 🏠

### 修改内容
- **console.html**：
  - 框选按钮选项呼出reorder mode时移除box-select id
  - 局部控制区域蓝色容器包裹（定时+确认+停止+倒计时），transform: scale(0.85)缩小15%
  - 定时输入框宽度50px→40px（缩短20%）
  - 变焦按钮：宽度170px容器，两个按钮平分（flex:1），height:24px（50%），padding:1px，font-size:11px，line-height:1.2，文字"变焦+"/"变焦-"
  - Home按钮：⌂→🏠 emoji
- **index.html**：UTF-8编码修复（sys.stdout.reconfigure）
- **engine.js updates**：remove confirmBtn references, use toggle reset

### E2E测试
- 22项PASSED，0FAILED

---
## v8.42 白平衡/对焦局部控制重新设计（2026-07-05）

### 设计方案
**整体布局**（所有元素同一行，宽度=下拉菜单宽度 100%）：
```
[框选] [定时] [输入框] min [开关] [停止]
```

**元素说明**：
1. **框选按钮**：单独样式，白色
2. **定时文字**："定时"（未输入时输入框也是placeholder="定时"）
3. **输入框**：
   - placeholder="定时"（未输入）
   - 只能输入数字（0-正整数）
   - font-size: 10px（文字更小）
   - padding: 4px 6px（更紧凑）
   - 提升精度：min-width:0 让flex子元素可以缩小
4. **min字样**：输入框右侧右侧的 "min" 字符
5. **开关**：.switch 样式，toggle checkbox
   - 左侧：应用开关
   - 关联逻辑：输入>0且开关打开 → 启动定时；开关关闭 → 停止定时
6. **停止按钮**：红按，flex-shrink:0

**应用逻辑**：
- 输入框输入数字
- 如果值为0：
  - 开关是灰色的（disabled）
  - 点击开关无效，提示"请输入大于0的分钟数"
- 如果值>0：
  - 打开开关：启动定时器
  - 关闭开关：停止定时器并提示

### 修改文件

#### 1. console.html
- **白平衡局部控制**：
  ```html
  <div id="wbLocalControls" style="display:none">
    <div style="display:flex;align-items:center;gap:4px;width:100%">
      <button id="btnWbRegionSelect" class="btn btn-secondary btn-sm" onclick="toggleRegionMode('whitebalance')" style="flex-shrink:0;padding:4px 8px">框选</button>
      <input type="text" id="wbTimerInput" value="" placeholder="定时" style="flex:1;min-width:0;padding:4px 6px;background:#0d1117;border:1px solid #30363d;border-radius:6px;color:#e0e6f0;font-size:10px" oninput="updateWbTimerInput(this)">
      <span style="color:#8b949e;font-size:10px;white-space:nowrap">min</span>
      <label class="switch" style="flex-shrink:0" title="打开应用定时">
        <input type="checkbox" id="wbTimerToggle" onchange="toggleWbTimer()">
        <span class="slider"></span>
      </label>
      <button id="btnWbLocalStop" class="btn btn-danger btn-sm" onclick="stopAutoWhiteBalance()" disabled style="opacity:0.5;flex-shrink:0;padding:4px 8px">停止</button>
    </div>
    <div id="wbTimerDisplay" style="color:#f85149;font-size:11px;text-align:center;margin-top:2px"></div>
  </div>
  ```
- **对焦局部控制**：同理，id改为focus

- **JS函数**：
  - remove 旧函数：
    - `updateWbConfirmBtn()`
    - `updateFocusConfirmBtn()`
    - `confirmWbTimer()`
    - `confirmFocusTimer()`
  - 新增函数：
    - `updateWbTimerInput(input)` - 纯数字输入过滤
    - `updateFocusTimerInput(input)` - 纯数字输入过滤
    - `toggleWbTimer()` - 开关控制白平衡定时
      - 输入<0 → 提示"请输入大于0的分钟数"
      - 区域未框选 → 提示"请先框选区域"
      - 开关打开 → 启动定时器
      - 开关关闭 → 停止定时器
    - `toggleFocusTimer()` - 同上，对焦

#### 2. router.py
- 全局变量 `_wb_searcher`/`_focus_searcher`
- 新增 `/api/v1/vision/whitebalance-stop` 端点
- 新增 `/api/v1/vision/focus-stop` 端点

#### 3. index.html
- **SSE事件处理**：原有的 confirmBtn 引用已清理，改为恢复 toggle 状态

### 备份
- `backup_20260705_0505/console.html`
- `backup_20260705_0505/index.html`

### 修改挑战
- **文本框宽度**：输入框 max-width:60px 导致右侧"min"和开关被挤出去
  - 修复：改用 `flex:1;min-width:0`，输入框自适应剩余空间
  - 同时 min-width:0 允许按钮 flex-shrink:0
- **停止按钮宽度**：`width:70%` 导致超出边界
  - 修复：去掉width属性，用 `flex-shrink:0;padding:4px 8px` 让按钮自适应内容宽度
- **Emoji vs 文字**：
  - 框选 → 框选（蓝色按钮）
  - 停止 → 停止（红色按钮）
  - 文字宽度略大于emoji，但不会超出边界
- **JavaScript精确替换**：
  - 旧函数在console.html中用Python脚本批量替换，避免文本匹配失败

---

## 白平衡基本事实（2026-07-05）📍

### 定位与供电（Voltage定位）
- Voltage值由前序环节设定，用于决策是否进入精调；本环节仅基于{R/B}增益与RGB差值
- 当前修复未涉及Voltage本身，也不修改设定逻辑

### Interleaved铭牌与阈值
- Interleaved铭牌列示：Δ=0.0228（Bronze，上游环节判断信号对准后发出）
- Interleaved铭牌不标注B=76→80的跨4步跳变；该跳变由best回滚逻辑解释
- 阈值设定未引入Interleaved铭牌，仅保留 Fine/GMS/Bronze 三态

### 精调轨迹与幂律行为
- 精调轨迹：B在局部范围内收敛，但未明确写入轨迹验证；当前修订省略轨迹验证以旧板为锚点
- 幂律验证未在当前修订中引入测温辅助；必要时可根据Version v8.40恢复轨迹验证流程

### v8.40修正点与时间
- 修正点1：精调阶段 Renounce"both"，改用R/B独立候选；同时进入stage=2时重置best，Context为{R/B}增益与ΔCloseG差值不标注电压/Voltage
- 修正点2：去除sub切换逻辑；取消"进入独立调整"的消息

### 验收与测试计划
- 语料检查：仅读取代码变更点，核对"both"已移除、sub逻辑省略
- E2E验证：捕捉精调阶段日志，验证 R = red候选、B = blue候选、无both；精调停在合理的fine_steps区间（≥10），若需引入超时仍可基于Interleaved铭牌与Fine/Bronze/GMS三态扩展

---

## 今日问题与解决方案

### 问题1：白平衡为什么没有最后的精调？
**原因：** best被粗调阶段记录，精调结果被丢弃。精调阶段sub=0有"both"候选，不符合要求。

**解决：** 精调阶段全部R/B独立 + 进入精调时重置best。

### 问题2：锁定白平衡逻辑？
**当前状态：** 已实现。每次白平衡完成后自动切换到 `locked` 模式。

**定时功能：** 已通过开关实现。输入时间>0，打开开关 → 启动定时器；关闭开关 → 停止定时器。

---

## 每日记录
- 2026-07-04: 主控台UI重构完成
- 2026-07-05: 精调逻辑修复 + 方案B停止功能 + UI布局调整 + 局部控制重新设计（v8.42）
- 阈值设定未引入Interleaved铭牌，仅保留 Fine/GMS/Bronze 三态

### 精调轨迹与幂律行为
- 精调轨迹：B在局部范围内收敛，但未明确写入轨迹验证；当前修订省略轨迹验证以旧板为锚点
- 幂律验证未在当前修订中引入测温辅助；必要时可根据Version v8.40恢复轨迹验证流程

### v8.40修正点与时间
- 修正点1：精调阶段 Renounce"both"，改用R/B独立候选；同时进入stage=2时重置best，Context为{R/B}增益与ΔCloseG差值不标注电压/Voltage
- 修正点2：去除sub切换逻辑；取消"进入独立调整"的消息

### 验收与测试计划
- 语料检查：仅读取代码变更点，核对"both"已移除、sub逻辑省略
- E2E验证：捕捉精调阶段日志，验证 R = red候选、B = blue候选、无both；精调停在合理的fine_steps区间（≥10），若需引入超时仍可基于Interleaved铭牌与Fine/Bronze/GMS三态扩展

---

## 今日问题与解决方案

### 问题1：白平衡为什么没有最后的精调？
**原因：** best被粗调阶段记录，精调结果被丢弃。精调阶段sub=0有"both"候选，不符合要求。

**解决：** 精调阶段全部R/B独立 + 进入精调时重置best。

### 问题2：锁定白平衡逻辑？
**当前状态：** 已实现。每次白平衡完成后自动切换到 `locked` 模式。

**定时功能：** 已通过开关实现。输入时间>0，打开开关 → 启动定时器；关闭开关 → 停止定时器。

---

## v8.43 修复（2026-07-05 07:22）

### 任务1：版本号更新
- constants.py: VERSION v8.39 → v8.43

### 任务2：预置点过滤
- ptz.py list_presets(): 只显示ID 1-32的预置点，隐藏设备自带预置点

### 任务3：操作日志刷新顺序统一
- index.html operationLog(): insertBefore → appendChild（与mediaLog统一，新内容在底部）

### 任务4：滤镜与日夜模式ISAPI控制
- 后端 router.py: 新增 GET/PUT `/api/v1/ptz/{device_id}/image/filter`
- 前端 console.html: 时域降噪下方增加2个三段式开关（日夜模式：日/夜/自动；IR滤镜：开/关/自动）
- 前端 index.html: 新增 .triple-switch/.ts-btn CSS
- ISAPI端点: `/ISAPI/Image/channels/1` 参数 dayNightMode/irCutFilter

---

## v8.44 重构（2026-07-05 08:39）

### 任务1：删除 region_analyzer.py
- 与 region_base.py 完全重复（_valid_pixels, _rgb_stats, calc_contrast），重构遗留的废弃文件
- 无任何引用

### 任务2：统一延时到 region_base.py
- region_base.py 重写：新增 `get_exposure_time(client)` + `calc_stable_delay(client)`
- 延时公式：曝光时间 × 2 + 0.5s基础延迟（BASE_STABLE_DELAY）
- 慢快门开启时，实际曝光 = 快门值 × 慢快门倍率
- whitebalance.py: 移除3处 sleep(0.3)，改用 calc_stable_delay
- autofocus.py: 移除1处 sleep(0.3)，改用 calc_stable_delay
- router.py: 移除 _capture_and_crop 中 sleep(0.1)，改用 calc_stable_delay

### 任务3：慢快门下拉菜单
- 后端: 新增 GET/PUT `/api/v1/ptz/{device_id}/image/slow-shutter`
- 前端: 运动控制→信息显示下方增加慢快门下拉菜单
- 选项: 关闭 / 1.25倍率 / 2倍率 / 4倍率 / 8倍率
- 设备不支持时灰色不可用（disabled）

### 任务4：曝光时间复用
- region_base.py 的 get_exposure_time() 从 /Image/channels/1/Shutter 读取 ShutterLevel
- 如果慢快门开启（minShutterLevelLimit ≠ maxShutterLevelLimit），计算实际曝光时间
- calc_stable_delay() 统一在 region_base.py 提供，所有搜索脚本复用

---

## 每日记录
- 2026-07-04: 主控台UI重构完成
- 2026-07-05 05:43: 精调逻辑修复 + 方案B停止功能 + UI布局调整 + 局部控制重新设计（v8.42）
- 2026-07-05 07:22: 版本号/预置点过滤/操作日志刷新/滤镜ISAPI控制（v8.43）
- 2026-07-05 15:49: 删除region_analyzer/统一延时calc_stable_delay/慢快门下拉菜单（v8.44）
# 2026-07-05 记忆

## 主控台界面重构 v8.39 ✅

### 完成的修改
1. **运动控制 - 中心按钮改为归位**
   - PTZ方向键中心按钮从停止(●)改为归位(⌂)
   - 调用 `ptzHome()` 回到10号预置点HOME
   - 删除预置点下方"归位"按钮

2. **运动控制 - OSD模块移动**
   - 标题改为"信息显示"
   - 保留两个开关：OSD(Info OSD) + PTZ
   - 从右侧画面控制区完整移动到左侧运动控制区
   - 统一ID：`infoOsdSwitch` / `ptzOsdSwitch`

3. **媒体操作 - 删除框选分析**
   - 移除"框选分析"section（白平衡/对焦按钮）
   - 框选功能现集成到白平衡/对焦的"局部"模式

4. **画面控制 - 删除Info OSD**
   - 右侧面板不再有OSD控制区
   - 所有OSD开关都在运动控制区

5. **白平衡/对焦模式重构**
   - 标签从"白平衡模式"改为"白平衡"/"对焦"
   - 3个选项：自动、手动、局部
   - 局部模式：自动切换到内置手动模式
   - 删除手动模式前置要求
   - 框选按钮 + 定时输入框（分钟，0=不执行）
   - 点击框选后显示停止按钮

6. **选区记忆功能**
   - 白平衡和对焦选区分别存储到localStorage
   - key: `region_whitebalance` / `region_focus`
   - 存储格式: `{x, y, w, h}` (归一化坐标)
   - 下次框选时恢复上次的选区

7. **触发与更新常量**
   - 更新 constants.py 版口号：v8.39
   - 更新 HEARDBEAT.md：v8.39 主控台UI重构完成

8. **测试验证**
   - 22项PASSED，0FAILED
   - 测试脚本 `e2e_v838_ui.py` 已更新至 v8.39

---
## v8.41 方案B：用户中断停止功能 ✅（2026-07-04）

### 修改文件
1. **whitebalance.py** — `_interrupted` 标志 + `_interrupt()` 方法 + run()中断检查（写入best R/B + 锁定模式 + yield interrupt事件）
2. **autofocus.py** — `_interrupted` 标志 + `_interrupt()` 方法 + run()中断检查（停止移动 + yield interrupt事件）
3. **router.py** — 全局变量 `_wb_searcher`/`_focus_searcher` + 新增 `/api/v1/vision/whitebalance-stop` + `/api/v1/vision/focus-stop` 端点
4. **console.html** — emoji按钮（🔲✅🛑）+ 停止按钮移到确认右侧 + 按钮状态管理 + 停止函数调用新端点 + operationLog('用户停止')
5. **index.html** — SSE监听 interrupt 事件 + 搜索启动时启用停止按钮 + done/error/interrupt/catch中恢复按钮灰色
6. **e2e_v838_ui.py** — 测试适配emoji按钮 + disabled状态检查 + UTF-8编码修复（sys.stdout.reconfigure）

### 按钮状态逻辑
| 按钮 | 条件 | 状态 |
|------|------|------|
| 框选 🔲 | 始终 | 可用 |
| 确认 ✅ | 定时值>0 且 定时器未运行 | 可用 |
| 确认 ✅ | 定时值<=0 或 定时器运行中 | 灰色不可用 |
| 停止 🛑 | 搜索流程运行中（SSE流中） | 可用 |
| 停止 🛑 | 搜索未启动 | 灰色不可用 |

### 备份
`backup_20260705_0327/`

---
## v8.41 UI布局调整（2026-07-05）

### 需求
1. 白平衡/对焦局部模式下：定时输入框 + 停止 + 确认按钮并列一排
2. 按钮缩小15%
3. 定时输入框缩短20%
4. 变焦功能恢复（速度滑动条下方）
5. 变焦按钮改为文字"变焦+"/"变焦-"，高度50%
6. Home按钮改为emoji 🏠

### 修改内容
- **console.html**：
  - 框选按钮选项呼出reorder mode时移除box-select id
  - 局部控制区域蓝色容器包裹（定时+确认+停止+倒计时），transform: scale(0.85)缩小15%
  - 定时输入框宽度50px→40px（缩短20%）
  - 变焦按钮：宽度170px容器，两个按钮平分（flex:1），height:24px（50%），padding:1px，font-size:11px，line-height:1.2，文字"变焦+"/"变焦-"
  - Home按钮：⌂→🏠 emoji
- **index.html**：UTF-8编码修复（sys.stdout.reconfigure）
- **engine.js updates**：remove confirmBtn references, use toggle reset

### E2E测试
- 22项PASSED，0FAILED

---
## v8.42 白平衡/对焦局部控制重新设计（2026-07-05）

### 设计方案
**整体布局**（所有元素同一行，宽度=下拉菜单宽度 100%）：
```
[框选] [定时] [输入框] min [开关] [停止]
```

**元素说明**：
1. **框选按钮**：单独样式，白色
2. **定时文字**："定时"（未输入时输入框也是placeholder="定时"）
3. **输入框**：
   - placeholder="定时"（未输入）
   - 只能输入数字（0-正整数）
   - font-size: 10px（文字更小）
   - padding: 4px 6px（更紧凑）
   - 提升精度：min-width:0 让flex子元素可以缩小
4. **min字样**：输入框右侧右侧的 "min" 字符
5. **开关**：.switch 样式，toggle checkbox
   - 左侧：应用开关
   - 关联逻辑：输入>0且开关打开 → 启动定时；开关关闭 → 停止定时
6. **停止按钮**：红按，flex-shrink:0

**应用逻辑**：
- 输入框输入数字
- 如果值为0：
  - 开关是灰色的（disabled）
  - 点击开关无效，提示"请输入大于0的分钟数"
- 如果值>0：
  - 打开开关：启动定时器
  - 关闭开关：停止定时器并提示

### 修改文件

#### 1. console.html
- **白平衡局部控制**：
  ```html
  <div id="wbLocalControls" style="display:none">
    <div style="display:flex;align-items:center;gap:4px;width:100%">
      <button id="btnWbRegionSelect" class="btn btn-secondary btn-sm" onclick="toggleRegionMode('whitebalance')" style="flex-shrink:0;padding:4px 8px">框选</button>
      <input type="text" id="wbTimerInput" value="" placeholder="定时" style="flex:1;min-width:0;padding:4px 6px;background:#0d1117;border:1px solid #30363d;border-radius:6px;color:#e0e6f0;font-size:10px" oninput="updateWbTimerInput(this)">
      <span style="color:#8b949e;font-size:10px;white-space:nowrap">min</span>
      <label class="switch" style="flex-shrink:0" title="打开应用定时">
        <input type="checkbox" id="wbTimerToggle" onchange="toggleWbTimer()">
        <span class="slider"></span>
      </label>
      <button id="btnWbLocalStop" class="btn btn-danger btn-sm" onclick="stopAutoWhiteBalance()" disabled style="opacity:0.5;flex-shrink:0;padding:4px 8px">停止</button>
    </div>
    <div id="wbTimerDisplay" style="color:#f85149;font-size:11px;text-align:center;margin-top:2px"></div>
  </div>
  ```
- **对焦局部控制**：同理，id改为focus

- **JS函数**：
  - remove 旧函数：
    - `updateWbConfirmBtn()`
    - `updateFocusConfirmBtn()`
    - `confirmWbTimer()`
    - `confirmFocusTimer()`
  - 新增函数：
    - `updateWbTimerInput(input)` - 纯数字输入过滤
    - `updateFocusTimerInput(input)` - 纯数字输入过滤
    - `toggleWbTimer()` - 开关控制白平衡定时
      - 输入<0 → 提示"请输入大于0的分钟数"
      - 区域未框选 → 提示"请先框选区域"
      - 开关打开 → 启动定时器
      - 开关关闭 → 停止定时器
    - `toggleFocusTimer()` - 同上，对焦

#### 2. router.py
- 全局变量 `_wb_searcher`/`_focus_searcher`
- 新增 `/api/v1/vision/whitebalance-stop` 端点
- 新增 `/api/v1/vision/focus-stop` 端点

#### 3. index.html
- **SSE事件处理**：原有的 confirmBtn 引用已清理，改为恢复 toggle 状态

### 备份
- `backup_20260705_0505/console.html`
- `backup_20260705_0505/index.html`

### 修改挑战
- **文本框宽度**：输入框 max-width:60px 导致右侧"min"和开关被挤出去
  - 修复：改用 `flex:1;min-width:0`，输入框自适应剩余空间
  - 同时 min-width:0 允许按钮 flex-shrink:0
- **停止按钮宽度**：`width:70%` 导致超出边界
  - 修复：去掉width属性，用 `flex-shrink:0;padding:4px 8px` 让按钮自适应内容宽度
- **Emoji vs 文字**：
  - 框选 → 框选（蓝色按钮）
  - 停止 → 停止（红色按钮）
  - 文字宽度略大于emoji，但不会超出边界
- **JavaScript精确替换**：
  - 旧函数在console.html中用Python脚本批量替换，避免文本匹配失败

---

## 白平衡基本事实（2026-07-05）📍

### 定位与供电（Voltage定位）
- Voltage值由前序环节设定，用于决策是否进入精调；本环节仅基于{R/B}增益与RGB差值
- 当前修复未涉及Voltage本身，也不修改设定逻辑

### Interleaved铭牌与阈值
- Interleaved铭牌列示：Δ=0.0228（Bronze，上游环节判断信号对准后发出）
- Interleaved铭牌不标注B=76→80的跨4步跳变；该跳变由best回滚逻辑解释
- 阈值设定未引入Interleaved铭牌，仅保留 Fine/GMS/Bronze 三态

### 精调轨迹与幂律行为
- 精调轨迹：B在局部范围内收敛，但未明确写入轨迹验证；当前修订省略轨迹验证以旧板为锚点
- 幂律验证未在当前修订中引入测温辅助；必要时可根据Version v8.40恢复轨迹验证流程

### v8.40修正点与时间
- 修正点1：精调阶段 Renounce"both"，改用R/B独立候选；同时进入stage=2时重置best，Context为{R/B}增益与ΔCloseG差值不标注电压/Voltage
- 修正点2：去除sub切换逻辑；取消"进入独立调整"的消息

### 验收与测试计划
- 语料检查：仅读取代码变更点，核对"both"已移除、sub逻辑省略
- E2E验证：捕捉精调阶段日志，验证 R = red候选、B = blue候选、无both；精调停在合理的fine_steps区间（≥10），若需引入超时仍可基于Interleaved铭牌与Fine/Bronze/GMS三态扩展

---

## 今日问题与解决方案

### 问题1：白平衡为什么没有最后的精调？
**原因：** best被粗调阶段记录，精调结果被丢弃。精调阶段sub=0有"both"候选，不符合要求。

**解决：** 精调阶段全部R/B独立 + 进入精调时重置best。

### 问题2：锁定白平衡逻辑？
**当前状态：** 已实现。每次白平衡完成后自动切换到 `locked` 模式。

**定时功能：** 已通过开关实现。输入时间>0，打开开关 → 启动定时器；关闭开关 → 停止定时器。

---

## 每日记录
- 2026-07-04: 主控台UI重构完成
- 2026-07-05: 精调逻辑修复 + 方案B停止功能 + UI布局调整 + 局部控制重新设计（v8.42）
- 阈值设定未引入Interleaved铭牌，仅保留 Fine/GMS/Bronze 三态

### 精调轨迹与幂律行为
- 精调轨迹：B在局部范围内收敛，但未明确写入轨迹验证；当前修订省略轨迹验证以旧板为锚点
- 幂律验证未在当前修订中引入测温辅助；必要时可根据Version v8.40恢复轨迹验证流程

### v8.40修正点与时间
- 修正点1：精调阶段 Renounce"both"，改用R/B独立候选；同时进入stage=2时重置best，Context为{R/B}增益与ΔCloseG差值不标注电压/Voltage
- 修正点2：去除sub切换逻辑；取消"进入独立调整"的消息

### 验收与测试计划
- 语料检查：仅读取代码变更点，核对"both"已移除、sub逻辑省略
- E2E验证：捕捉精调阶段日志，验证 R = red候选、B = blue候选、无both；精调停在合理的fine_steps区间（≥10），若需引入超时仍可基于Interleaved铭牌与Fine/Bronze/GMS三态扩展

---

## 今日问题与解决方案

### 问题1：白平衡为什么没有最后的精调？
**原因：** best被粗调阶段记录，精调结果被丢弃。精调阶段sub=0有"both"候选，不符合要求。

**解决：** 精调阶段全部R/B独立 + 进入精调时重置best。

### 问题2：锁定白平衡逻辑？
**当前状态：** 已实现。每次白平衡完成后自动切换到 `locked` 模式。

**定时功能：** 已通过开关实现。输入时间>0，打开开关 → 启动定时器；关闭开关 → 停止定时器。

---

## v8.43 修复（2026-07-05 07:22）

### 任务1：版本号更新
- constants.py: VERSION v8.39 → v8.43

### 任务2：预置点过滤
- ptz.py list_presets(): 只显示ID 1-32的预置点，隐藏设备自带预置点

### 任务3：操作日志刷新顺序统一
- index.html operationLog(): insertBefore → appendChild（与mediaLog统一，新内容在底部）

### 任务4：滤镜与日夜模式ISAPI控制
- 后端 router.py: 新增 GET/PUT `/api/v1/ptz/{device_id}/image/filter`
- 前端 console.html: 时域降噪下方增加2个三段式开关（日夜模式：日/夜/自动；IR滤镜：开/关/自动）
- 前端 index.html: 新增 .triple-switch/.ts-btn CSS
- ISAPI端点: `/ISAPI/Image/channels/1` 参数 dayNightMode/irCutFilter

---

## v8.44 重构（2026-07-05 08:39）

### 任务1：删除 region_analyzer.py
- 与 region_base.py 完全重复（_valid_pixels, _rgb_stats, calc_contrast），重构遗留的废弃文件
- 无任何引用

### 任务2：统一延时到 region_base.py
- region_base.py 重写：新增 `get_exposure_time(client)` + `calc_stable_delay(client)`
- 延时公式：曝光时间 × 2 + 0.5s基础延迟（BASE_STABLE_DELAY）
- 慢快门开启时，实际曝光 = 快门值 × 慢快门倍率
- whitebalance.py: 移除3处 sleep(0.3)，改用 calc_stable_delay
- autofocus.py: 移除1处 sleep(0.3)，改用 calc_stable_delay
- router.py: 移除 _capture_and_crop 中 sleep(0.1)，改用 calc_stable_delay

### 任务3：慢快门下拉菜单
- 后端: 新增 GET/PUT `/api/v1/ptz/{device_id}/image/slow-shutter`
- 前端: 运动控制→信息显示下方增加慢快门下拉菜单
- 选项: 关闭 / 1.25倍率 / 2倍率 / 4倍率 / 8倍率
- 设备不支持时灰色不可用（disabled）

### 任务4：曝光时间复用
- region_base.py 的 get_exposure_time() 从 /Image/channels/1/Shutter 读取 ShutterLevel
- 如果慢快门开启（minShutterLevelLimit ≠ maxShutterLevelLimit），计算实际曝光时间
- calc_stable_delay() 统一在 region_base.py 提供，所有搜索脚本复用

---

## v8.46 设备参数记忆功能（2026-07-05 20:54-21:42）

### 需求
根据设备MAC作为唯一标识，自动记忆所有参数配置（白平衡模式、慢快门、降噪、亮度快门等），下次连接同一设备自动恢复最后设置。

### 已完成的修改

| 文件 | 修改内容 |
|------|----------|
| `src/advanced/device_settings.py` | 新增：`save_device_settings()` / `load_device_settings()` |
| `src/api/router.py` | 8个端点添加自动保存：whitebalance, noisereduce, exposure, shutter, iris, sharpness, slow_shutter, image_settings |
| `src/api/router.py` | 新增GET `/ptz/{device_id}/image/settings-saved` 端点 |
| `src/web/includes/console.html` | 所有set函数在 `_isInitializingImageParams=true` 时抑制toast |
| `src/web/includes/console.html` | 新增 `restoreSettings()` 函数，设备重连时自动恢复参数 |
| `src/main/constants.py` | VERSION v8.45→v8.46 |

### 支持的参数
白平衡模式 + R/B增益、降噪（空域/时域）、曝光模式、快门、光圈、锐度、慢快门（DSS）、亮度/对比度/饱和度

### 存储路径
`data/devices/{mac}/settings.json`

### settings.json 示例
```json
{
  "whitebalance": {"mode": "manual", "red_gain": 200, "blue_gain": 51},
  "noisereduce": {"spatial_level": 75, "temporal_level": 6},
  "slow_shutter": {"dss_level": "*4", "enabled": true},
  "exposure": {"mode": "manual"},
  "iris": {"level": 160},
  "sharpness": {"level": 60},
  "image_settings": {"brightness": 55, "contrast": 52, "saturation": 58},
  "last_updated": "2026-07-05T13:49:00.681772+00:00"
}
```

### 测试结果
- ✅ 设置保存：所有参数通过API设置后自动保存到settings.json
- ✅ settings-saved端点：正常返回保存的设置数据
- ✅ Toast抑制：初始化阶段（`_isInitializingImageParams=true`）不弹toast
- ✅ 快门设置：设备端返回HTTP 400（设备限制，非代码bug）

### 🔴 待修复问题
**restoreSettings() 只设DOM值，未调用API应用到设备**

| 参数 | DOM设置 | API调用 | 状态 |
|------|---------|---------|------|
| 白平衡模式 | ✅ `onWbModeChange()` | ✅ | 正常 |
| 白平衡R/B增益 | ✅ 设值 | ❌ 未调`setWhiteBalanceGain()` | 需修复 |
| 降噪 | ✅ 设值 | ❌ 未调`setNoiseReduce()` | 需修复 |
| 慢快门 | ✅ `onSlowShutterChange()` | ✅ | 正常 |
| 快门 | ✅ 设值 | ❌ 未调`setShutter()` | 需修复 |
| 光圈 | ✅ 设值 | ❌ 未调`setIris()` | 需修复 |
| 曝光模式 | ✅ 设值 | ❌ 未调`setExposureMode()` | 需修复 |
| 锐度 | ✅ 设值 | ❌ 未调`setSharpness()` | 需修复 |
| 画面设置 | ✅ 设值 | ❌ 未调PUT settings API | 需修复 |

### 修复方案
修改 `restoreSettings()` 函数（console.html约856行），在设置DOM值后显式调用对应的API函数：
- 白平衡增益 → `setWhiteBalanceGain(channel, value)`
- 降噪 → `setNoiseReduce(channel, value)`
- 快门 → `setShutter(value)`
- 光圈 → `setIris(value)`
- 曝光模式 → `setExposureMode(mode)`
- 锐度 → `setSharpness(value)`
- 画面设置 → `apiPut('/api/v1/ptz/' + deviceIp + '/image/settings', imgData)`

### 备份
`backup_20260705_205538/`

### 每日记录
- 2026-07-05 20:54: 开始v8.46设备参数记忆功能开发
- 2026-07-05 21:38: 后端保存+读取完成，前端restoreSettings()需修复API调用

# 2026-06-25 开发日志

## Limit + Speed 方向信息修复

### 问题发现
1. 删除 `_detect_movement_direction()` 后同时删除了 `direction_info` 字段
2. speed.py 第480行阻塞检查导致测试直接返回失败
3. 前端 `renderAdvResult()` 缺少 speed 结果渲染分支

### 教训
- **删除字段时必须检查下游依赖** — speed.py 依赖 `direction_info`
- **方向信息不需要独立测试** — P6.3/P6.4 readings 已包含方向数据
- **数据源头提供，消费者只读** — limit 测方向，speed 读取

### 修复内容

**limit.py test_pan_limits()**
- 添加 first_pan/pan_increasing 变量
- 循环内判断方向（第一次有效变化 >10 时记录趋势）
- 输出 `positive_command_increases` 到 result

**limit.py test_tilt_limits_and_flip()**
- 添加 first_tilt/tilt_increasing 变量
- 循环内判断方向
- 输出 `positive_command_increases` 到 result

**limit.py run_all_tests()**
- 从 pan_limits 和 tilt_limits 提取 `positive_command_increases`
- 构造 direction_info = {pan: {...}, tilt: {...}}
- 添加到 result dict

**speed.py**
- 恢复阻塞检查：`if "direction_info" not in limit_data: return {...}`
- 提取 pan_dir/tilt_dir 用于起点判断

**advanced.html renderAdvResult()**
- 添加 speed 结果渲染分支（档位、测试数、Pan/Tilt速度表）

**limit.py restore_device()**
- 删除 sleep(5)
- 改用 goto_home() 替代 goto_preset(10)

### 最终结构

```
P6.3 test_pan_limits()
  → 输出 positive_command_increases
  
P6.4 test_tilt_limits_and_flip()
  → 输出 positive_command_increases
  
run_all_tests()
  → direction_info = {
      pan: {positive_command_increases: <从P6.3提取>},
      tilt: {positive_command_increases: <从P6.4提取>}
    }
  → 保存到 limit.json
  
speed.py
  → 检查 direction_info 存在
  → 用于起点位置判断
```